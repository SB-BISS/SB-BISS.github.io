import { Keypair } from '../types';
import { HashType } from './types';
interface Options {
    hashType: HashType;
}
/**
 * @name secp256k1Sign
 * @description Returns message signature of `message`, using the supplied pair
 */
export default function secp256k1Sign(message: Uint8Array | string, { secretKey }: Partial<Keypair>, { hashType }?: Partial<Options>): Uint8Array;
export {};
